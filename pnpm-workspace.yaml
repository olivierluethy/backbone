packages:
  - "packages/*"
  - "examples/*"

# Vite/esbuild only power the demo frontend, which Backbone never needs to run in order
# to analyse. Skip its build scripts to keep installs deterministic and quiet.
allowBuilds:
  esbuild: false
