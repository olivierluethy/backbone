{
  "name": "backbone",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic frontend-to-backend generator. Analyze a React+TS frontend, review an editable Blueprint, generate a runnable Node or PHP backend. No AI at analyze or generate time.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "bb": "pnpm --filter @backbone/cli exec bb",
    "web": "pnpm --filter @backbone/web dev",
    "web:server": "pnpm --filter @backbone/web server"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
