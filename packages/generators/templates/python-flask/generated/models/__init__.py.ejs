"""Model package — imports every model so `db.metadata` is complete before create_all().

Generated by Backbone — do not edit (regenerated wholesale).

Importing this package pulls in all SQLAlchemy model classes, which is what registers their
tables on the shared metadata. `init_db()` imports this module for exactly that side effect.
"""
<% if (auth) { -%>
from .user import User  # noqa: F401
<% } -%>
<% for (const e of view.entities) { -%>
from .<%= e.varName %> import <%= e.className %>  # noqa: F401  (table: <%= e.table %>)
<% } -%>
