<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * Migration: create the `<%= j.name %>` pivot table for the many-to-many relation between
 * `<%= j.left %>` and `<%= j.right %>`.
 *
 * Holds the two foreign keys (<%= j.leftFk %> -> <%= j.left %>, <%= j.rightFk %> -> <%= j.right %>)
 * as a composite primary key; rows cascade-delete with either parent. OWNED by Backbone — do not
 * hand-edit.
 */
return new class extends Migration
{
    /** Create the `<%= j.name %>` pivot table. */
    public function up(): void
    {
        Schema::create('<%= j.name %>', function (Blueprint $table): void {
            $table->foreignId('<%= j.leftFk %>')->constrained('<%= j.left %>')->cascadeOnDelete();
            $table->foreignId('<%= j.rightFk %>')->constrained('<%= j.right %>')->cascadeOnDelete();
            $table->primary(['<%= j.leftFk %>', '<%= j.rightFk %>']);
        });
    }

    /** Drop the `<%= j.name %>` pivot table. */
    public function down(): void
    {
        Schema::dropIfExists('<%= j.name %>');
    }
};
