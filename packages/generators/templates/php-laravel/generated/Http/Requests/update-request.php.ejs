<?php

declare(strict_types=1);

namespace App\Generated\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

/**
 * Validation rules for updating a <%= e.name %> (PUT/PATCH /<%= e.table %>/{id}).
 *
 * Every field is prefixed with `sometimes`, so a partial payload is accepted: only the fields
 * actually present are validated (and then applied). The underlying type rules match the create
 * request. Generated by Backbone — do not edit.
 */
class Update<%= e.className %>Request extends FormRequest
{
    /** Authorisation is left open here; gate it in your own middleware or policy. */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * The validation rules for a partial <%= e.name %> update.
     *
     * @return array<string, string>
     */
    public function rules(): array
    {
        return [
<% for (const c of e.writable) { -%>
            '<%= c.name %>' => '<%= lv.rule(c, true) %>',
<% } -%>
        ];
    }
}
