<?php

declare(strict_types=1);

namespace App\Generated\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

/**
 * Validation rules for creating a <%= e.name %> (POST /<%= e.table %>).
 *
 * Every writable column is validated; non-nullable columns are `required`, nullable ones are
 * `nullable`. The rules are derived directly from the Blueprint field types (integer/numeric/
 * boolean/date/enum/…). Generated by Backbone — do not edit.
 */
class Store<%= e.className %>Request extends FormRequest
{
    /** Authorisation is left open here; gate it in your own middleware or policy. */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * The validation rules for a new <%= e.name %>.
     *
     * @return array<string, string>
     */
    public function rules(): array
    {
        return [
<% for (const c of e.writable) { -%>
            '<%= c.name %>' => '<%= lv.rule(c, false) %>',
<% } -%>
        ];
    }
}
