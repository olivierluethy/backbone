<?php

/**
 * Application service providers — YOURS to extend (written once, never overwritten).
 *
 * Laravel 11 auto-discovers package providers; only your own application providers need to be
 * listed here. This lean scaffold registers none by default. Add e.g.
 * `App\Providers\AppServiceProvider::class` when you introduce one.
 */

return [
    //
];
