import { Router } from "express";
<% for (const e of view.entities) { %>import { <%= e.varName %>Router } from "./<%= e.varName %>/<%= e.varName %>.routes";
<% } %><% if (auth) { %>import { authRouter } from "../shared/auth/auth.routes";
<% } %>
/** Mounts every feature module's router. Generated — do not edit. */
export const apiRouter = Router();
<% if (auth) { %>
apiRouter.use(authRouter);
<% } %><% for (const e of view.entities) { %>apiRouter.use(<%= e.varName %>Router);
<% } %>