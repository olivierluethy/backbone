import "dotenv/config";

/** Typed environment access. Generated — do not edit (regenerated on every run). */
export const env = {
  PORT: Number(process.env.PORT ?? 4000),
  DB_CLIENT: process.env.DB_CLIENT ?? "better-sqlite3",
  DB_FILE: process.env.DB_FILE ?? "./data.sqlite",
  DB_HOST: process.env.DB_HOST ?? "127.0.0.1",
  DB_PORT: Number(process.env.DB_PORT ?? 3306),
  DB_USER: process.env.DB_USER ?? "root",
  DB_PASSWORD: process.env.DB_PASSWORD ?? "",
  DB_NAME: process.env.DB_NAME ?? "backbone",
<% if (auth) { %>  JWT_SECRET: process.env.JWT_SECRET ?? "change-me-in-production",
  JWT_EXPIRES_IN: process.env.JWT_EXPIRES_IN ?? "7d",
<% } %>};
