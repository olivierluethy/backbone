# Server
PORT=4000

# Database — SQLite by default (zero setup). For MySQL, set DB_CLIENT=mysql2 and the vars below.
DB_CLIENT=better-sqlite3
DB_FILE=./data.sqlite
# DB_CLIENT=mysql2
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=backbone
<% if (auth) { %>
# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
<% } %>
