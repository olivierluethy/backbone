"""Environment configuration. Generated by Backbone — do not edit."""
import os

from dotenv import load_dotenv

load_dotenv()


class Settings:
    DATABASE_URL: str = os.getenv("DATABASE_URL", "sqlite:///./data.sqlite")
    PORT: int = int(os.getenv("PORT", "4200"))
<% if (auth) { -%>
    JWT_SECRET: str = os.getenv("JWT_SECRET", "change-me-in-production")
    JWT_ALGORITHM: str = "HS256"
    JWT_EXPIRES_MINUTES: int = int(os.getenv("JWT_EXPIRES_MINUTES", "10080"))
<% } -%>


settings = Settings()
