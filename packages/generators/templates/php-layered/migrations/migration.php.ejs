<?php

declare(strict_types=1);

/**
 * <%= isInit ? "Initial schema." : "Additive migration." %> Generated by Backbone.
 * Additive only — never edit or delete this file; regeneration appends new migrations.
 * The runner (bin/migrate.php) executes each statement in `up` once, in filename order.
<% if (removedNotes && removedNotes.length) { -%>
 *
 * Not applied (additive migrations never drop):
<% for (const n of removedNotes) { -%>
 *   - <%= n %>
<% } -%>
<% } -%>
 */

return [
    'up' => [
<% for (const stmt of statements) { -%>
        <<<'SQL'
<%- stmt %>
SQL,
<% } -%>
    ],
];
